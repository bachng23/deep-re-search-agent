"""Contrast feature (not implemented yet).

Reasons across fetched papers to surface research gaps — the space
*between* papers, not summaries of them. Will expose a `find_gaps`
node (reasoning tier) that fills `ResearchState.gaps`.

Planned layout: node.py, prompts.py, schemas.py.
"""
